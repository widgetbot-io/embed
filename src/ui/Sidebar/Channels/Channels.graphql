query Channels($guild: String!) {
  guild(id: $guild) {
    channels {
      __typename

      name
      id
      ... on TextChannel {
        nsfw
        parent {
          name
        }
      }
      ... on NewsChannel {
        nsfw
      }
    }
  }
}
