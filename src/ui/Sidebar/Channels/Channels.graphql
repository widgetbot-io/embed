query Channels($guild: String!) {
  guild(id: $guild) {
    channels(type: "TextChannel") {
      name
      id
      ... on TextChannel {
        parent {
          name
        }
      }
    }
  }
}
