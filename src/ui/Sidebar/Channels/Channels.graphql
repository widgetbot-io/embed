query Channels($guild: String!) {
  guild(id: $guild) {
    channels {
      __typename

      name
      id
      ... on TextChannel {
        canSend
        nsfw
        category
      }
      ... on NewsChannel {
        canSend
        nsfw
        category
      }
    }
  }
}
