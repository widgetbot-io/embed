query Channels($guild: String!) {
  guild(id: $guild) {
    channels {
      __typename

      name
      id
      ... on TextChannel {
        canSend
        nsfw
        parent {
          name
        }
      }
      ... on NewsChannel {
        canSend
        nsfw
      }
    }
  }
}
