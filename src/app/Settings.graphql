query Settings($guild: String!) {
  guild(id: $guild) {
    settings {
      guestMode
      readonly
      theme {
        css
        colors {
          primary
          accent
          background
        }
      }
    }
  }
}
