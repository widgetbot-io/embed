query Settings($guild: String!) {
  guild(id: $guild) {
    settings {
      guestMode
      singleChannel
      readonly
      theme {
        css
        colors {
          primary
          accent
          background
        }
      }
    }
  }
}
