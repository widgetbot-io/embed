query Settings($guild: String!) {
  guild(id: $guild) {
    id
    settings {
      guestMode
      singleChannel
      readonly
      theme {
        css
        colors {
          primary
          accent
          background
        }
      }
    }
  }
}
