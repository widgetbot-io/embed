name: "Build and Deploy"
on: pull_request
jobs:
  docker-build-publish:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Submodules
        run: git submodule update --init --recursive --remote
      - uses: actions/setup-node@v1
        with:
          node-version: '12.x'
      - name: Build
        run: yarn && yarn build
      - name: Docker login
        run: echo ${{ secrets.GITHUB_TOKEN }} | docker login docker.pkg.github.com --username yomanz --password-stdin
      - name: Docker build
        run: docker build -t docker.pkg.github.com/widgetbot-io/embed/staging:${{github.sha}} .
      - name: Docker push tag latest
        run: docker push docker.pkg.github.com/widgetbot-io/embed/staging:${{github.sha}}
